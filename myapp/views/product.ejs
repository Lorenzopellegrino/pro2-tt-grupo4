<!DOCTYPE html>
<html lang="es">

<%- include("./partials/head") %>

<body>
	<%- include("./partials/header") %>

	<!-- product detail -->
	<div class="container products-wrapper">
		<div class="d-flex align-items-center">
			<h2 class="products-title">Nombre del producto</h2> 
			<p class="product-detail-description">
				<a href="/product/add">Editar</a>
			</p>
		</div>
		<div class="product-detail">
			<div class="row">
				<article class="col-12 col-lg-8 product-detail-info">
					<img class="product-detail-img" src=<%= productos[0].imagen% %> alt="">
                    <p class="product-detail-description"><%= productos[0].descripcion %></p>
                </article>
                <div class="col-12 col-lg-4">
                    <section class="comments">
                        <% for(let index = 0; index < prodcutos[0].comentarios.length; index++) {%>
                        <article>
                            <img src=<%= productos[0].comentarios[index].foto_de_perfil %> alt="">
                            <div class="info">
                                <p class="usuario"><strong> <a href="/users/profile"><%= productos[0].comentarios[index].user%></a></strong> <%= productos[0].comentarios[index].comentario %> </p>
                            </div>
                        </article>
                        <%}
                    </section>
                

					</section>
					<form class="form" action="" method="POST">
						<input class="form-input" type="text" value="" placeholder="Agregar comentario">
						<button type="submit" class="buy-now-button">AGREGAR COMENTARIO</button>
					</form>
					<hr>
				</div>
			</div>
		</div>
	</div>
	<!-- /product detail -->

	<%- include("./partials/footer") %>


</body>	
</html>